# 📁 Sélection du Root Directory - Guide Visuel

## 🎯 Vous voyez une liste de dossiers

Dans la liste déroulante "Root Directory", vous devriez voir :
- `Guinea-smart-Electricity` (racine)
- `back` (dossier backend)
- `project` (dossier frontend) ⭐ **C'EST CELUI-CI !**

## ✅ Action à Faire

1. **Cliquez sur la liste déroulante** "Root Directory"

2. **Sélectionnez** : `project` (pas `Guinea-smart-Electricity`, pas `back`)

3. **Vérifiez** que le champ affiche bien : `project`

## 📋 Si vous ne voyez pas "project" dans la liste

Si `project` n'apparaît pas dans la liste :
1. Cliquez sur le champ
2. Tapez manuellement : `project`
3. Ou sélectionnez la racine et modifiez après le déploiement dans Settings

## ⚠️ Important

- ❌ **NE sélectionnez PAS** : `Guinea-smart-Electricity` (racine)
- ❌ **NE sélectionnez PAS** : `back` (backend)
- ✅ **Sélectionnez** : `project` (frontend)

## 🎯 Après Sélection

Une fois `project` sélectionné :
- Cliquez sur **"Deploy"** ou **"Continue"**
- Le déploiement commencera

---

**Résumé** : Dans la liste, choisissez `project` !

